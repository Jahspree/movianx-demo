import ExperienceDC from '../movianx/generated/ExperienceDC';

export const metadata = {
  title: 'Explore — Movianx Immersive Experiences',
  description:
    'Entertainment was never meant to be passive. Watch, listen, explore and belong across the Movianx ecosystem.',
};

// EXPLORER LANDING (/explore) — approved "Movianx Experience" design.
export default function ExplorePage() {
  return <ExperienceDC />;
}
