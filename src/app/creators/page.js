import LandingV2DC from '../movianx/generated/LandingV2DC';

export const metadata = {
  title: 'Movianx for Creators',
  description: 'Where creativity becomes reality. Create, stream and own — the AI-native creator ecosystem.',
};

// CREATOR LANDING (/creators) — approved "Movianx Landing V2" design.
export default function CreatorsPage() {
  return <LandingV2DC />;
}
